title: Boesen Counseling
email: eboesen@gmail.com
baseurl: "" # the subpath of the site, e.g. /blog
url: "http://boesencounseling.com" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
plugins:
    - jekyll-redirect-from
whitelist:
    - jekyll-redirect-from
exclude: [ruby, Gemfile, Gemfile.lock, vendor]
include:
    - _pages
